import { Rule } from '../rule';
import { Parser } from '../parser';
import { Issue } from '../issue';
/**
 * Rule to ensure tags are properly closed.
 */
export declare class StructureRule extends Rule {
    private parser;
    init(parser: Parser): void;
    finalise(): Issue[];
}
